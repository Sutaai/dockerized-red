version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"
  jobs:
    pre_build:
      # Python is just used for building the docs.
      - pip install .

formats: all

mkdocs:
  configuration: mkdocs.yml
